[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python manage.py migrate && python manage.py collectstatic --noinput && python manage.py loaddata fixtures/local_data.json && gunicorn hks_backend.wsgi --bind 0.0.0.0:$PORT"
restartPolicyType = "ON_FAILURE"

